# Core dependencies (required)
streamlit>=1.30.0
pandas>=2.0.0
openpyxl>=3.0.0
pyyaml>=6.0

# Visualization (required for simulation charts and map view)
plotly>=6.0.0

# Geospatial (required for precinct map and KeplerGL export)
geopandas>=1.0.0
shapely>=2.0.0
pyogrio>=0.7.0
pyproj>=3.7.0
