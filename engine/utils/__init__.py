# engine/utils/__init__.py
"""
engine/utils — Shared utility modules for all engine subsystems.

Modules:
  helpers.py           — _g(), find_latest_csv(), BASE_DIR, load_yaml(), load_json()
  derived_data_reader.py — DerivedDataReader for run/contest-aware file resolution
"""
