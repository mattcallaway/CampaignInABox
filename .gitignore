# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv
venv/
env/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.suo
*.user

# ── Voter Intelligence (Prompt 11) — NEVER COMMIT VOTER DATA ─────────────────
# Individual voter records — strictly local only
data/voters/
data/voter_files/
derived/voter_models/
derived/voter_segments/

# Voter universe files contain aggregate counts only — SAFE to commit
# derived/voter_universes/ is intentionally NOT excluded

# Raw historical election downloads (large, re-downloadable)
data/elections/CA/*/detail*.xls
data/elections/CA/*/detail*.xlsx
data/elections/CA/*/detail*.xls*

# Parquet voter snapshots — local only
*.parquet


# Temp / scratch
_scaffold.ps1
tmp/
scratch/
*.tmp
*.bak

# Jupyter
.ipynb_checkpoints/

# Prompt 14 — War Room runtime data (never commit — may contain sensitive campaign data)
data/campaign_runtime/*
!data/campaign_runtime/**/.gitkeep
derived/war_room/private/*

# Large raw data (use LFS rules above for GIS)
# raw xlsx/xls vote files ARE committed (small; source of truth)
 
# Campaign Private Data Restrictions
/data/voters/
/data/campaign_runtime/
/data/private/
/data/intelligence/private/

# Historical Archive
# Allowed: models/, derived/archive/, reports/archive/
# Ignored: data/election_archive/raw_unnormalized/ (if any)


# Prompt 20.8 � Persistent session store (local only, never committed)
data/local_sessions/

